import { EmitorStatusPipe } from './emitor-status.pipe';

describe('EmitorStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new EmitorStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
